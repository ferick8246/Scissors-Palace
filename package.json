{
  "name": "scissors-palace",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "graphql": "^15.5.1",
    "stripe": "^8.167.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd react-ui/ && npm start\"",
    "install": "cd server && npm i && cd ../react-ui/ && npm i && npm run seed",
    "build": "cd react-ui/ && npm run build"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
